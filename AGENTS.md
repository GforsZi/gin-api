# Gon API Project

## Build & Run
- **Run:** `go run main.go`
- **Build:** `go build -o api .`

## Architecture
- Simple single-file Go service using the `gin` framework.
- Main entrypoint: `main.go`
- Listens on `localhost:8080`.
